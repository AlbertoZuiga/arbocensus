\chapter{RESULTADOS OBTENIDOS Y POSIBLES MEJORAS}

\section{RESULTADOS OBTENIDOS}
\subsection{Métricas de Calidad de Rutas}

% DRAFT — revisar
La calidad de la solución producida por el motor de optimización se evalúa sobre un
\textit{dataset} real de $N =$ [DATO PENDIENTE] árboles correspondiente a la zona de
[DATO PENDIENTE: zona/comuna], empleando los parámetros temporales por defecto
($T_{\min} = 7\,200$\,s y $T_{\max} = 10\,800$\,s) y el tiempo de servicio configurado
por árbol. Se reportan cinco indicadores: el número de rutas $k$ generado de forma
automática por el solver, el tiempo promedio por ruta $\bar{T}$, la desviación
estándar de los tiempos de ruta ---cuyo criterio de aceptación es que no supere el
15\,\% del promedio, en línea con el objetivo de balance de carga---, el número de
rutas que exceden la cota dura $T_{\max}$ ---que debe ser cero por construcción--- y el
tiempo de cómputo del solver. La Tabla~\ref{tab:metricas} resume estos resultados.

\begin{table}[H]
  \centering
  \caption{Métricas de calidad de rutas sobre el dataset de evaluación.}
  \label{tab:metricas}
  \begin{tabularx}{\textwidth}{lXX}
    \toprule
    Métrica & Valor & Criterio \\
    \midrule
    Número de rutas $k$ & [DATO PENDIENTE] & mínimo posible \\
    Tiempo promedio por ruta & [DATO PENDIENTE] & $T_{\min} \leq \bar{T} \leq T_{\max}$ \\
    Desviación estándar & [DATO PENDIENTE] & $< 15\%$ del promedio \\
    Rutas que exceden $T_{\max}$ & [DATO PENDIENTE] & $= 0$ \\
    Tiempo de cómputo (solver) & [DATO PENDIENTE] & $< 5$ min \\
    \bottomrule
  \end{tabularx}
\end{table}

% NOTA: medir sobre el dataset real de la tesis ejecutando un OptimizationJob y
% leyendo total_routes, la media/desviación de total_estimated_time_sec de las Routes,
% y el tiempo de cómputo del solver (log del pipeline).

% ---- 3.1.2 ------------------------------------------------------
\subsection{Comparación con Solución Greedy}

% TODO (copilot-prompts.md → §3.1.2):
% Comparar Arbocensus (OR-Tools VRP) vs baseline greedy (vecino más cercano).
% Métricas: tiempo total de desplazamiento, balance (std dev), N de rutas.
% El baseline se genera programáticamente con el mismo dataset.

% DRAFT — revisar
Como referencia de comparación se define una solución \textit{greedy} basada
en el heurístico del vecino más cercano, que se generará programáticamente sobre el
mismo \textit{dataset} y con los mismos parámetros temporales ($T_{\min}$, $T_{\max}$ y
tiempo de servicio) que la solución de OR-Tools. Este \textit{baseline} construirá
rutas encadenando en cada paso el árbol no visitado más cercano hasta agotar el
presupuesto temporal de la ruta, sin optimización global ni balance explícito, lo que
permitirá cuantificar el valor agregado del solver frente a una heurística voraz. La
Tabla~\ref{tab:comparativa} contrastará ambos enfoques.

\begin{table}[H]
  \centering
  \caption{Comparación entre la solución de OR-Tools y el baseline greedy.}
  \label{tab:comparativa}
  \begin{tabularx}{\textwidth}{lXX}
    \toprule
    Métrica & OR-Tools (VRP) & Greedy (vecino más cercano) \\
    \midrule
    Número de rutas $k$ & [DATO PENDIENTE] & [DATO PENDIENTE] \\
    Tiempo total de desplazamiento & [DATO PENDIENTE] & [DATO PENDIENTE] \\
    Desviación estándar de $T_r$ & [DATO PENDIENTE] & [DATO PENDIENTE] \\
    Rutas que exceden $T_{\max}$ & [DATO PENDIENTE] & [DATO PENDIENTE] \\
    \bottomrule
  \end{tabularx}
\end{table}

% NOTA: generar el baseline greedy programáticamente sobre el dataset real y comparar
% total_travel_time_sec, desviación estándar de los tiempos por ruta y k contra la
% RoutingSolution de estrategia global.

% ---- 3.1.3 -----------------------------------------------------
\subsection{Baseline de calidad geográfica de rutas (VRP global sin clustering)}

Antes de evaluar un preprocesamiento de \textit{clustering} geográfico, se
caracterizó la calidad espacial de la solución producida por la formulación
actual: un único \textit{Open mTSP} global resuelto con OR-Tools, sin
agrupamiento previo de los nodos. El interés no es el balance temporal ---ya
controlado por la penalización blanda sobre $T_{\min}$--- sino la
\textit{compacidad geográfica} de cada ruta y el grado de
\textit{solapamiento} (\textit{interleaving}) entre rutas distintas.

Se definieron los siguientes indicadores sobre las coordenadas reales de las
paradas (\texttt{RouteStop}), usando distancia de \textit{haversine}:

\begin{itemize}
  \item \textbf{Compacidad} ($\sum r_{\max}$): suma, sobre las rutas, del radio
  máximo de cada ruta (distancia del centroide a su parada más lejana). Menor es mejor.
  \item \textbf{Solapamiento} (\textit{interleaving}): número de paradas
  pertenecientes a \textit{otras} rutas que caen dentro de la caja envolvente
  (\textit{bounding box}) de una ruta dada, sumado sobre todas las rutas. Se
  reporta tanto el total como su valor normalizado por número de rutas
  ($\overline{\text{solap}} = \text{total}/k$), para permitir comparar entre
  tamaños de dataset. Mayor indica peor partición espacial.
  \item \textbf{IoU peor par}: intersección sobre unión de las cajas
  envolventes del par de rutas con mayor superposición. Mayor indica mayor
  solapamiento espacial entre dos rutas.
\end{itemize}

El balance temporal se reporta como $\min_r T_r / \max_r T_r$
(\textit{balance\_score} del modelo). Se ejecutó un barrido reproducible
mediante el comando \texttt{baseline\_sweep} ($5$ semillas $\times$ $3$ tamaños
de dataset, $n \in \{20, 40, 80\}$) sobre la zona de Providencia/Santiago
Centro, con distribución uniforme de nodos y el límite de solver por defecto de
$180$\,s, promediando las métricas por semilla (Tabla~\ref{tab:baseline-geo}).

\begin{table}[H]
  \centering
  \caption{Baseline de calidad geográfica del VRP global (media de 5 semillas por $n$).}
  \label{tab:baseline-geo}
  \begin{tabularx}{\textwidth}{rXXXXXX}
    \toprule
    $n$ & Rutas $k$ & Balance & $\sum r_{\max}$ [m] & Solap. total & Solap./ruta & IoU peor par \\
    \midrule
    20 & 3.0 & 0.946 & 6\,530  & 0.4  & 0.13 & 0.01 \\
    40 & 5.0 & 0.949 & 8\,758  & 6.6  & 1.32 & 0.13 \\
    80 & 8.0 & 0.872 & 13\,126 & 25.8 & 3.23 & 0.28 \\
    \bottomrule
  \end{tabularx}
\end{table}

El hallazgo principal es un \textbf{desacople entre balance temporal y calidad
geográfica}: el balance se mantiene alto ($0.87$--$0.95$) mientras el
solapamiento entre rutas crece de forma monótona con la densidad, tanto en
términos absolutos ($0.4 \to 6.6 \to 25.8$) como normalizado por ruta
($0.13 \to 1.32 \to 3.23$). El IoU del peor par sigue la misma tendencia
($0.01 \to 0.13 \to 0.28$). El efecto es \textbf{sistémico y dependiente de la
densidad}: con $n=20$ el solapamiento es prácticamente nulo, pero crece de
forma sostenida al aumentar la densidad de nodos, observándose el caso
recurrente de una ruta compacta geográficamente contenida dentro de la caja
envolvente de otra más extensa. Esto es coherente con que el modelo optimiza
tiempo total con un término de balance blando, sin ningún criterio espacial
explícito en la partición.

% TODO: comparar este baseline contra la variante con clustering geográfico
% como preprocesamiento, una vez implementada (misma métrica vía baseline_sweep).

% ---- 3.2 -------------------------------------------------------
\section{POSIBLES MEJORAS}

% TODO (copilot-prompts.md → §3.2):
% Describir las mejoras V2 identificadas: portal del censador mobile-first,
% importación GeoJSON y legacy DB, chunking OSRM para datasets >5000 puntos,
% análisis de sensibilidad del tiempo de solver, Playwright E2E.

% DRAFT — revisar
Entre las extensiones identificadas para versiones futuras del sistema se encuentran:

\begin{itemize}
  \item \textbf{Disjunciones por nodo}: incorporar restricciones de disjunción (\texttt{AddDisjunction}) con penalización, de modo que el solver pueda descartar árboles atípicos (\textit{outliers}) geográficamente aislados cuando su inclusión degrade excesivamente la compacidad de una ruta.
  \item \textbf{Matrices de costos de gran escala}: fragmentación (\textit{chunking}) de consultas OSRM para \textit{datasets} que superen los 5\,000 puntos activos, superando el límite \texttt{--max-table-size} de una única solicitud.
  \item \textbf{Operación offline robusta}: reemplazar la cola offline básica actual por un \textit{service worker} que gestione el almacenamiento de teselas y la sincronización diferida de forma confiable en condiciones de conectividad intermitente.
  \item \textbf{Importación de formatos adicionales}: soporte GeoJSON e importación directa desde bases de datos legadas del sistema Arbocensus previo.
  \item \textbf{Análisis de sensibilidad}: estudio sistemático de la relación entre el tiempo límite del solver y la calidad de la solución.
  \item \textbf{Pruebas de extremo a extremo}: automatización de los flujos completos de administrador y censador mediante Playwright.
\end{itemize}

Asimismo, se documentan las siguientes limitaciones aceptadas en el alcance del
producto actual: los tokens JWT se almacenan en \texttt{localStorage}, lo que
simplifica la implementación a costa de una menor protección frente a ataques de tipo
\textit{cross-site scripting}; y la construcción de la matriz de costos no aplica
fragmentación, por lo que el tamaño de \textit{dataset} queda acotado por el límite de
tabla de una única consulta OSRM.

